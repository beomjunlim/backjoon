#include <iostream>
using namespace std;

int main()
{
  int a, n;
  cin >> a;
  cin >> n;
  int sum = 0;
  for (int i = 0; i < n; i++)
  {
    int x, y;
    cin >> x;
    cin >> y;
    sum += (x * y);
  }
  if (sum == a)
    cout << "Yes" << endl;
  else
    cout << "No" << endl;
}
