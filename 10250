#include <iostream>
using namespace std;

int main() {
	int k;
	int h, w, n;
	int sum;
	cin >> k;

	for (int i = 0; i < t; i++) {
		cin >> h >> w >> n;

		if (n%h == 0) {
			sum = h * 100 + (n / h);
		}
		else {
			sum = (n%h) * 100 + (n / h) + 1;
		}
		cout << sum <<"\n";
	}
}
