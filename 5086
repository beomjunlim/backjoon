#include <iostream>
#include <string>
using namespace std;

int main()
{
  int a, b;
  do
  {
    cin >> a;
    cin >> b;
    if (a == 0 && b == 0)
      break;
    if (a % b == 0)
      cout << "multiple" << endl;
    else if (b % a == 0)
      cout << "factor" << endl;
    else
      cout << "neither" << endl;

  } while (1);
}
